syntax = "proto3";
package resources;

import "google/protobuf/empty.proto";

service Resources {
    rpc GetResources(google.protobuf.Empty) returns (ResourcesReply);
}

message ResourcesReply {
    int64 cpu = 1;
    int64 memory = 2;
}